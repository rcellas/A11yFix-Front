/**
 * Environment configuration template.
 * Copy this file to `environment.ts` and set local variables.
 * Note: `environment.ts` and `environment.*.ts` are ignored by git to protect credentials.
 */
export const environment = {
  production: false,
  apiUrl: '/api'
};
