export const environment = {
  production: false,
  /**
   * Base URL for the a11yfix-api (NestJS) backend.
   * During local development, requests to /api can be proxied to http://localhost:3000
   * or targeted directly.
   */
  apiUrl: '/api'
};
